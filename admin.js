const req=JSON.parse(localStorage.getItem("lessing_requests")||"[]"), chats=JSON.parse(localStorage.getItem("lessing_contact")||"[]");document.getElementById("reqCount").textContent=req.length;document.getElementById("chatCount").textContent=chats.length;document.getElementById("adminRequests").innerHTML=req.length?req.map(x=>`<div class="stat"><b>${x.first} ${x.last}</b><p>${x.class} · ${x.area} · ${x.date}<br>Code: ${x.code}</p></div>`).join(""):"Keine Anfragen.";document.getElementById("adminChats").innerHTML=chats.length?chats.map(x=>`<div class="stat"><p>${x.text}</p></div>`).join(""):"Noch keine Nachrichten.";document.getElementById("adminErrors").innerHTML="Keine KI-Fehler gemeldet.";