/*
  Optional shared backend configuration.
  Leave empty for browser-only fallback mode.
  For real multi-device admin/chat sync, fill these values with YOUR backend project.
*/
window.LESSING_CONFIG = window.LESSING_CONFIG || {
  supabaseUrl: '',
  supabaseAnonKey: '',
  bigAdminEmail: ''
};
